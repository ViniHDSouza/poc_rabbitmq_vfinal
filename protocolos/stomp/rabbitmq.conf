# Carrega a topologia completa (vhosts, users, exchanges, queues, bindings)
# a partir do definitions.json no startup do broker.
#
# O definitions.json DEVE incluir vhosts e users porque load_definitions
# roda ANTES das env vars (RABBITMQ_DEFAULT_USER) serem processadas.
load_definitions = /etc/rabbitmq/definitions.json
